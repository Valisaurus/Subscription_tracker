type NotificationsProps = {
  setActivePopUp: React.Dispatch<React.SetStateAction<string>>;
};
const Notifications = ({ setActivePopUp }: NotificationsProps) => {
  return <div></div>;
};

export default Notifications;
